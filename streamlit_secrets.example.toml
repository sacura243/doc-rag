# Do not upload a real secrets.toml file to GitHub.
# In Streamlit Cloud, paste these values into: App settings -> Secrets.
appid = "YOUR_XFYUN_APPID"
apikey = "YOUR_XFYUN_APIKEY"
apisecret = "YOUR_XFYUN_APISECRET"

# Keep the same defaults as the local configuration.
embed_backend = "local"
ws_url = "wss://spark-api.xf-yun.com/v1.1/chat"
domain = "lite"
temperature = 0.3
